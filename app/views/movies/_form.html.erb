<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
   
  <div class="form-group">
    <%= form.label 'Movie Name', style: "display: block" %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label 'Client', style: "display: block" %>
    <%= form.select :client_id, options_for_select(@clients, @movie.client_id), {}, { class: 'form-control' } %>
  </div>

  <div>
    <%= form.submit "Submit", class:"btn btn-success btn-sm mt-2" %>
  </div>
<% end %>
